import { View, Text } from 'react-native';
import React from 'react';
import st from '../styles';

const NameComponent = () => {
  return ( 
    <View style={st.shadowStyle}>
      <Text>Samuel Forrest</Text>
    </View>
    );
}

export default NameComponent